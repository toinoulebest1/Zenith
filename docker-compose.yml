services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "0.0.0.0:8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped

networks:
  default:
    driver: bridge
